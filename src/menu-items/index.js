import { dashboard, userdashboard } from './dashboard';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard],
  userItems: [userdashboard]
};

export default menuItems;
